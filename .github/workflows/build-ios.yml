name: Build iOS Unsigned IPA (Cloud Fetch)

on:
  workflow_dispatch: 

jobs:
  build-ios:
    runs-on: macos-latest

    steps:
      - name: Fetch Original Code
        uses: actions/checkout@v4
        with:
          repository: liuchuancong/pure_live
          ref: master

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Install Flutter dependencies
        run: |
          mkdir -p build/ios/SourcePackages
          mkdir -p build/macos/SourcePackages
          flutter pub get

      - name: Patch Vision Pro Bug in Plugin
        run: |
          find ~/.pub-cache/hosted/pub.dev/device_info_plus* \( -name "*.m" -o -name "*.h" \) -type f -exec sed -i '' 's/isiOSAppOnVision/isLowPowerModeEnabled/g' {} +

      # 终极防御手术：一枪封死所有可能导致 iOS 14 闪退的环境缺陷
      - name: Comprehensive iOS 14 Backwards Compatibility Patch
        run: |
          gem install xcodeproj
          ruby -e '
          require "xcodeproj"
          
          # 1. 改造 Podfile：强行统一所有第三方插件的最低运行版本和动态库嵌入设置
          podfile_path = "ios/Podfile"
          if File.exist?(podfile_path)
            content = File.read(podfile_path)
            macro = "flutter_additional_ios_build_settings(target)"
            if content.include?(macro)
              patch = "flutter_additional_ios_build_settings(target)\n      target.build_configurations.each do |c|\n        c.build_settings[\"IPHONEOS_DEPLOYMENT_TARGET\"] = \"13.0\"\n        c.build_settings[\"ALWAYS_EMBED_SWIFT_STANDARD_LIBRARIES\"] = \"YES\"\n        c.build_settings[\"ENABLE_BITCODE\"] = \"NO\"\n      end"
              content.gsub!(macro, patch)
              File.write(podfile_path, content)
            end
          end
          
          # 2. 改造 Xcode 主工程配置
          project = Xcodeproj::Project.open("ios/Runner.xcodeproj")
          runner = project.targets.find { |t| t.name == "Runner" }
          ext = project.targets.find { |t| t.name == "ShareExtension" }
          
          # A. 修复原项目 Bundle ID 错位缺陷
          if runner && ext
            runner_config = runner.build_configurations.find { |c| c.name == "Release" }
            runner_id = runner_config.build_settings["PRODUCT_BUNDLE_IDENTIFIER"] || "com.mystyle.purelive.liu"
            ext.build_configurations.each do |c|
              c.build_settings["PRODUCT_BUNDLE_IDENTIFIER"] = "#{runner_id}.ShareExtension"
            end
          end
          
          # B. 移除死循环构建脚本
          if runner
            runner.build_phases.delete_if { |phase| phase.isa == "PBXShellScriptBuildPhase" && phase.name == "Thin Binary" }
          end
          
          # C. 遍历所有工程组件，注入终极免死设置
          project.targets.each do |target|
            target.build_configurations.each do |config|
              config.build_settings["IPHONEOS_DEPLOYMENT_TARGET"] = "13.0"
              config.build_settings["ALWAYS_EMBED_SWIFT_STANDARD_LIBRARIES"] = "YES"
              config.build_settings["ENABLE_BITCODE"] = "NO"
              
              # 补全 RPATH 路径确保 dyld 运行时能顺利找到自带的补丁库
              rpaths = config.build_settings["LD_RUNPATH_SEARCH_PATHS"] || ["$(inherited)"]
              rpaths = [rpaths] if rpaths.is_a?(String)
              rpaths << "@executable_path/Frameworks" << "@loader_path/Frameworks"
              config.build_settings["LD_RUNPATH_SEARCH_PATHS"] = rpaths.uniq
              
              # 专门为主程序注入底层链接器参数（将新系统才有的库声明为弱链接，防止启动时被击杀）
              if target.name == "Runner"
                ldflags = config.build_settings["OTHER_LDFLAGS"] || ["$(inherited)"]
                ldflags = [ldflags] if ldflags.is_a?(String)
                
                # 弱链接拆分/新增的 Framework
                ldflags << "-weak_framework" << "AVFAudio"
                ldflags << "-weak_framework" << "CoreMIDI"
                ldflags << "-weak_framework" << "UniformTypeIdentifiers"
                
                # 弱链接 Swift 现代高级库（全面封锁 Concurrency/String/Regex 等秒退源）
                ldflags << "-Wl,-weak-lswift_Concurrency"
                ldflags << "-Wl,-weak-lswift_StringProcessing"
                ldflags << "-Wl,-weak-lswift_RegexParser"
                
                config.build_settings["OTHER_LDFLAGS"] = ldflags.uniq
              end
            end
          end
          project.save
          '

      - name: Install iOS Pods
        run: |
          cd ios
          pod install --repo-update
          cd ..

      - name: Build unsigned iOS app
        run: flutter build ios --release --no-codesign

      - name: Package into .ipa
        run: |
          mkdir -p build/ios/iphoneos/Payload
          mv build/ios/iphoneos/Runner.app build/ios/iphoneos/Payload/
          cd build/ios/iphoneos
          zip -r pure_live.ipa Payload

      - name: Upload IPA Artifact
        uses: actions/upload-artifact@v4
        with:
          name: pure_live-ios-app
          path: build/ios/iphoneos/pure_live.ipa
